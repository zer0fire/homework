import walk from "./walk";

// TODO 通过 walk 获取对应的 for \ if
// 创建对应代码模板，比如 for -> while, if -> switch, if -> while true break
// magic string 取代对应的字符串
function analyze() {
  //   walk();
}
export default analyze;
